# Tooling for building and publishing the package (see create_dist.sh, upload_dist.sh)
# Not needed to run the app - install alongside requirements.txt for local dev:
#   pip install -r requirements.txt -r dev-requirements.txt
build>=1.6.1
twine>=7.0.0
